{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "apiVersion": {
      "type": "number"
    },
    "id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "version": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "author": {
      "type": "string"
    },
    "runtime": {
      "type": "string",
      "enum": [
        "node",
        "dotnet"
      ],
      "description": "插件运行时类型（双轨制）：   - \"node\"（默认）：entry = .cjs/.js/.mjs，宿主进程内 require 加载，     共享主进程生命周期（存量生态，行为不变）   - \"dotnet\"：entry = 插件自带 .exe（framework-dependent，依赖     .NET 10 Desktop Runtime），宿主以独立子进程运行并经 stdio     JSON 行协议通信——进程级隔离、崩溃不影响主进程、工具调用     走 IPC。性能敏感/系统级插件（采样、本地推理）推荐此轨。"
    },
    "entry": {
      "type": "string"
    },
    "icon": {
      "type": "string"
    },
    "settingsPanel": {
      "type": "string"
    },
    "settingsSection": {
      "$ref": "#/definitions/PluginSettingsSection"
    },
    "defaultEnabled": {
      "type": "boolean"
    },
    "deps": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/PluginCapability"
      }
    }
  },
  "required": [
    "apiVersion",
    "id",
    "name",
    "version",
    "description",
    "author",
    "entry"
  ],
  "additionalProperties": false,
  "description": "插件作者提交的 manifest 原始形状，是 Manifest JSON Schema 的生成目标。 与 PluginManifest 的区别：defaultEnabled 可省略（宿主归一化为 true）， 字段格式约束（id 连字符、SemVer、entry 裸文件名等）由加载器的文件 与格式校验补充完成，Schema 只负责结构、类型和枚举。",
  "definitions": {
    "PluginSettingsSection": {
      "type": "string",
      "enum": [
        "channels",
        "plugins"
      ],
      "description": "插件设置面板的挂载分区。每开放一个枚举值，设置页必须有对应的 挂载容器，所以首版故意只收窄到渠道和插件两个分区。"
    },
    "PluginCapability": {
      "type": "string",
      "enum": [
        "channels",
        "llm",
        "secrets",
        "workspace",
        "conversations",
        "scheduler",
        "speech-input"
      ],
      "description": "插件可向宿主申请的宿主服务。deps 只是服务可用性声明，不是安全权限： 未声明的服务不会注入，声明的服务也只是获得该服务的稳定接口。"
    }
  }
}
